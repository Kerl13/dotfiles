#!/bin/sh


if [ "$(hostname)" != "aji" ]; then
    echo 'hum, vraiment ?'
fi


rsync \
    --exclude-from=/home/martin/.config/backup-exclude.txt \
    -cavz /home/martin \
    chyper:/home/martin/backup
